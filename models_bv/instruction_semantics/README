## About

This folder contains instruction semenatics. These define the semantics of each instruction at the ISA level along with the hardware state changes, thus each set of semantics also describes the microarchitectural model of execution.
